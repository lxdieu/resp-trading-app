import Layout from "@/src/components/common/Layout";

const DashboardPage = () => {
  return (
    <Layout loading={false}>
      <div>dashboard</div>
    </Layout>
  );
};

export default DashboardPage;
