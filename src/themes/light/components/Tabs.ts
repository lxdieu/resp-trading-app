import colors from "../../colors";

const Tabs = {
  styleOverrides: {
    root: {
      border: `2px solid ${colors.sb20}`,
      borderRadius: 4,
      overflow: "hidden",
    },
  },
};

export default Tabs;
